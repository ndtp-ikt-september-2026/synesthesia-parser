[project]
name = "synesthesia-parser"
version = "0.1.0"
description = "Pop-Music gear scraper with MiniLM vectorization and OpenCart LiveStore ingestion"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "curl-cffi>=0.16.3",
    "fake-useragent>=2.2.0",
    "pydantic>=2.13.5",
    "sentence-transformers>=6.0.1",
    "textual>=8.2.8",
]

[tool.uv]
package = false
